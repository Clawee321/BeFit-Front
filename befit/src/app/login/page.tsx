import LoginContainer from "@/components/login/LoginContainer";
import React from "react";

const page = () => {
  return (
    <div>
      <LoginContainer />
    </div>
  );
};

export default page;
